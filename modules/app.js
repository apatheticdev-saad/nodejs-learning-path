const { log } = require('console');
const math = require('./math'); // little bit different from python
console.log(math.add(5, 3));

console.log(math.sub(10,5));
console.log(math.sub(20,10));

console.log(math.mul(5,2));
